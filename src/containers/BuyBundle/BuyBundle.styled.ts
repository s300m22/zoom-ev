import styled from 'styled-components';
import { StepsLineWrapper } from '../../elements/StepsLine/StepsLine.styled';

const PageLeadContentWrapper = styled.div`
  ${StepsLineWrapper} {
    width: 100%;
    max-width: 540px;
    margin: 0 auto;
  }
`;

export default PageLeadContentWrapper;
