import styled from 'styled-components';

const VinTipContent = styled.div`
  ul {
    padding-inline-start: 0;
    li {
      font-size: 14px;
      margin: 10px 20px;
    }
  }
`;

export default VinTipContent;
